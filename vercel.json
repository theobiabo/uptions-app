{
	"$schema": "https://openapi.vercel.sh/vercel.json",
	"installCommand": "bun install",
	"buildCommand": "bun run build",
	"outputDirectory": "packages/app/dist/client",
	"rewrites": [
		{
			"source": "/:path*",
			"destination": "/_shell.html"
		}
	]
}
